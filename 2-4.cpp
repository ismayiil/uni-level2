#include <bits/stdc++.h>

using namespace std;
#define fi(l,r) for(int i = l; i < r; i++)
#define sz(v) v.size()
signed main(){

    int a; cin >> a;
    if(a % 2) cout << "odd";
    else cout << "even";
    
}